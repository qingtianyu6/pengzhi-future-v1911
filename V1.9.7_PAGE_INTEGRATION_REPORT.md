# 棚智未来 v1.9.7 页面整合说明

本版本以 v1.9.6 完整前后端工程为底座，保留原 FastAPI 后端、数据库、业务工作台与 API 调用层。

## 已替换页面
- `/home`：使用“1比1复刻网站”首页参考图版本，并将导航/登录/注册/游客/进入平台热点接入真实路由。
- `/introduction`：替换为“平台介绍页 React 前端 修复空白页版”。
- `/features`：替换为“核心功能页 React 前端”。
- `/highlights`：替换为 `pengzhi-future-frontend` 技术亮点页。
- `/scenarios`：替换为“应用场景页 React 前端 Hero最终稳定版”。
- `/results`：继续使用 v1.9.6 原项目成果页。

## 接口与路由
- 所有新版页面的顶部导航均已改为 React Router 真路由，不再是无效按钮或页内假跳转。
- 游客模式、登录、注册、进入管理平台均接入 `/platform` 工作台入口。
- 新版页面搜索按钮通过统一事件调用主站搜索浮层。
- 原 `/data`、`/environment`、`/diagnosis`、`/production` 等兼容链接与 `/api` 请求保持不变。
- FastAPI 生产环境 SPA 回退、`/uploads`、`/assets` 托管保持不变。

## 样式隔离
独立页面原始 CSS 已按页面根节点进行作用域隔离，避免 `.brand`、`.hero`、`.site-header` 等通用类名污染后台工作台或其他页面。

## 交付包精简说明
为确保整合版便于下载与部署，本交付包保留全部运行源码、当前页面素材、V0.1 环境模型与 V0.2 部署模型；未打包原 V1.9.6 中的 visual_qa 截图、已被新版页面替代的旧营销页素材副本、根目录内重复素材 ZIP，以及 V0.2 训练阶段的 research_rich / par_research / residual_* / plots 中间产物。这些内容不参与当前网站运行或 API 推理。
