# V1.9.11 修正说明

本版基于 V1.9.10，修复用户截图中两个明确问题：

1. **“智慧大棚 · 看得见的改变”错位**
   - 根因：主工程旧版全局样式里仍存在一个不带作用域的 `.highlight-section { display:grid; ... }`，与新版首页同名类发生 CSS 冲突。
   - 结果：新版首页的“看得见的改变”整个 section 被旧规则强制拆成两列，导致内容挤在左边，中央大棚和右侧卡片看起来消失/错位。
   - 修复：对 `.home-v11 .highlight-section` 显式重置为 block，并重新锁定 outcome 三栏布局；同时取消中央大棚图的 multiply 混合和 cover 裁切，保证图片始终可见。

2. **鼠标滚轮不能正常控制整页上下**
   - 根因：工程历史样式同时存在 `.site-home { overflow:hidden }`、`.reference-site-home { overflow:hidden }`，后续又叠加 html/body 双滚动容器；加上弹层通过内联 body overflow 锁定，容易形成浏览器滚动归属不稳定。
   - 修复：改成单一浏览器滚动容器：`html` 负责纵向滚动，`body/#root/公开页容器`保持 visible；横向使用 `overflow-x: clip`，不再创建额外滚动容器。
   - 搜索/移动菜单仅在真正打开时给 `html` 添加 `site-scroll-locked`，关闭后自动移除，不再修改 body 内联 overflow。

其余页面、统一导航、后端接口和管理平台业务逻辑不变。
